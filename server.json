{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.sidyellur/tether",
  "description": "Local-first agent memory over SQLite: remember, recall, link, forget. Degrades, never fails.",
  "repository": {
    "url": "https://github.com/sidyellur/tether",
    "source": "github"
  },
  "websiteUrl": "https://github.com/sidyellur/tether",
  "version": "0.6.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "tether-memory",
      "version": "0.6.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "TETHER_DB",
          "description": "Path to the SQLite memory file. Defaults to the platform data dir (e.g. ~/.local/share/tether/memory.db).",
          "isRequired": false,
          "format": "filepath"
        },
        {
          "name": "TETHER_SEMANTIC",
          "description": "Set to 0 to force keyword-only recall. Semantic recall needs the [semantic] extra and is on by default when available.",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "TETHER_SYNC_URL",
          "description": "Optional libSQL/Turso primary URL to sync memory across devices. Leave unset for local-only.",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "TETHER_SYNC_TOKEN",
          "description": "Auth token for TETHER_SYNC_URL.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}
